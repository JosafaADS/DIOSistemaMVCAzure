@model IEnumerable<SistemaMVC.Models.Pessoa>
@{
    ViewData["Title"] = "Lista de Pessoas";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5 text-primary">👥 @ViewData["Title"]</h1>
        <a asp-action="Criar" class="btn btn-success btn-lg shadow-sm">
            <i class="bi bi-plus-circle"></i> Nova Pessoa
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Nome</th>
                        <th>Sobrenome</th>
                        <th>Telefone</th>
                        <th>Email</th>
                        <th class="text-center">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="align-middle fw-bold">@item.Nome</td>
                            <td class="align-middle">@item.Sobrenome</td>
                            <td class="align-middle">@item.Telefone</td>
                            <td class="align-middle">@item.Email</td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <a asp-action="Editar" asp-route-id="@item.Id" class="btn btn-outline-primary btn-sm">Editar</a>
                                    <a asp-action="Detalhes" asp-route-id="@item.Id" class="btn btn-outline-info btn-sm">Detalhes</a>
                                    <a asp-action="Excluir" asp-route-id="@item.Id" class="btn btn-outline-danger btn-sm" onclick="return confirm('Tem certeza?')">Excluir</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
